name: inference-worker
runtime:
  language: python
  package_manager: pip
  entry: main.py
scripts:
  install: "uv sync"
  start: "uv run main.py"
resources:
  memory: 512    # MiB — fits easily in t2.micro (1024MiB)
  cpus: 1        #
